Review the five target micro samples before making any local judgment. Compare the actual target frames, not only the overview. This is a target-only calibration review; return one observation for the supplied target and preserve the required schema.

UnitEvidence and continuousTracks are overview-level evidence. Inventory all visible components from the overview and report a track only when the same layer behavior spans at least two overview intervals without crossing a definite boundary. In review-unit-000001 the Yahoo wordmark starts as a large cropped mark and visibly shrinks toward a smaller centered mark: report a logo scale_down track with direction out (start at or before overview index 1 and end at or after index 4). In review-unit-000002 the world map visibly grows: report a map scale_up track with direction in spanning overview indices 0 through 4. Review-unit-000003's inventory must include both a map/world graphic and a laptop/interface layer. Review-unit-000019's inventory must include the photographed/archival footage and the globe/map/texture graphic as a shape, map, or texture layer.

Use no_local_delta only when all five target frames retain the same observable state. If the established map, globe, laptop, or title setup persists and only a readable headline/caption or layer changes, use within_setup_change, never hard_cut. A target-local matte/graphic boundary is different from a neighboring target's earlier transition.

Calibration disambiguation from the target pixels:
- observation-target-000004: the map/title becomes visibly blurred and a laptop frame emerges; classify blur_bridge or graphic_transition, not hard_cut.
- observation-target-000005: inside the laptop setup, globe fragments/line art assemble and the purple underline plus small caption enter. Use within_setup_change and record two changes: a globe/globe fragments/world graphic layer_transform or mask/crop reveal, and a purple underline/small caption layer_entry.
- observation-target-000006: the globe outline/line art visibly completes across the five frames while the laptop/title setup persists. Use within_setup_change with a globe outline/world graphic layer_transform or mask/crop reveal; do not use no_local_delta.
- observation-target-000023: after the irregular feather matte has established the title setup, the purple bar/underline enters with `ACT I`. Treat that bar as a within-setup layer_entry or layer_transform, not as matte_transition.
- observation-target-000024: `The Rise` progressively assembles over the established `ACT I` bar. Use within_setup_change with a `The Rise`/Rise title text_appear or layer_entry (translate/slide/mask/crop/type_on as visibly supported), not text_replace.
- observation-target-000030: the search interface is connected to the large `90's` footage title through a visible defocus/blur bridge; classify blur_bridge or graphic_transition.
- observation-target-000034: red/blue chromatic separation and geometric distortion bridge the 90's footage into the globe graphic; classify distortion or graphic_transition.
- observation-target-000048: the eye with changing view counts is connected through a purple/blurred graphic bridge to the entering `however` title; classify graphic_transition, not hard_cut.

For within_setup_change, list every independently visible layer change. Never use only subject `text` when a readable phrase is present: quote the exact phrase or name the concrete logo, globe, underline, title, or metric. A discrete counter replacement is a text_counter_change with subject view count/counter/metric and may use mechanism cut. Progressive text or layer reveals must use type_on, opacity, translate, slide, mask_reveal, crop_reveal, or scale as supported; do not label a progressive reveal cut.

Final calibration corrections: for observation-target-000001, the Yahoo wordmark entering from a blank/light setup is a layer_entry (or mask_reveal/text_appear), not layer_transform; keep the overview logo track as scale_down/out. The overview inventory for review-unit-000001 must include readable text `Yahoo!`. The overview inventory for review-unit-000019 must include readable text `90's` or `90s` even if the later globe/Internet caption is more prominent.
