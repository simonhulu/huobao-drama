Review the five target micro samples before making any local judgment. Compare the actual target frames, not only the overview.

Use no_local_delta when the composition is materially unchanged across all five target frames. Do not treat compression noise, tiny camera drift, or an already-completed animation as a local edit. Use within_setup_change only when the same established setup remains and one or more visible layers change. Use hard_cut when a new visual setup replaces the previous setup at a discrete boundary. Use graphic_transition or blur_bridge when a graphic or blur bridge visibly connects two setups. Use matte_transition for an irregular matte or shape reveal from black. Use ambiguous when the sampled boundary does not resolve cut versus fade or another transition family. Do not call a fade unless intermediate partial opacity is visibly supported.

For every within_setup_change, list each independently visible change that matters. Name concrete subjects from the frame: recognizable brand marks should be called a logo or wordmark, maps and globes should be named as such, readable headlines and captions should be quoted, and UI numbers should include their semantic role (for example view count, counter, or metric) plus the visible values when possible. Avoid generic subjects such as only Text, shape, or background when the frame supplies a more specific noun. The mechanism must describe the observed motion or reveal, not a guess: use cut only for a discrete replacement, type_on only for progressive character appearance, and scale or translate only when the layer visibly changes size or position.

Build unitEvidence from the overview as a static inventory. Record all independently identifiable layers, including map, interface, shape, text, logo, still imagery, live-action footage, and archival footage when present. Quote readable visible text exactly. Add continuousTracks only for a single layer whose behavior clearly spans at least two overview intervals; do not invent a track from a single target or let a track cross a definite transition boundary.
