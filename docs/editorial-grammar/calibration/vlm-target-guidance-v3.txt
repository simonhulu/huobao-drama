Review the five target micro samples before making any local judgment. Compare the actual target frames, not only the overview.

This is a target-only review with one target micro group and five overview images. Return exactly one targetObservations item for the supplied target. UnitEvidence and continuousTracks are still OVERVIEW-level evidence, not target-local events: inventory every visible overview layer and readable word, and include a continuous track whenever one layer's behavior is visibly established across at least two overview intervals (endSampleIndex - startSampleIndex >= 2). Do not invent a track from the target frames or let a track cross a definite boundary. A target-only review is allowed to repeat the same overview track that another target in the same review would report.

Use no_local_delta when the composition is materially unchanged across all five target frames. Use within_setup_change when the established underlying setup persists and a layer changes. If the same map, globe, graphic, or background remains while only a headline or caption is replaced, this is within_setup_change with a text_replace, layer_replace, or text_appear change, never hard_cut. A previous target's matte or graphic transition does not carry into a later target whose five frames are already inside the established setup. Use hard_cut only when a new underlying setup directly replaces the old one with no bridge in these five frames. Use graphic_transition or blur_bridge when a graphic/blur bridge visibly connects two setups. Use matte_transition only for the target-local irregular aperture or shaped reveal that replaces one setup with another. Do not call a fade unless intermediate partial opacity is visibly supported.

For a globe or line-art assembly, name globe, globe outline, globe fragments, world graphic, or line art and record layer_transform, layer_entry, mask_reveal, or crop_reveal as supported. For a colored bar or underline that enters or moves inside an established title setup, name purple bar or purple underline and record layer_entry or layer_transform; do not reclassify the target as a matte transition. For a title that progresses from ACT I to The Rise, name ACT I/The Rise and record text_appear or layer_entry. For a search interface changing into a large 90's title, or an eye/count graphic giving way to however, use graphic_transition or blur_bridge when the frames show a designed/blurred bridge, not hard_cut.

For every within_setup_change, list each independently visible change. Never use only the subject "text" when a readable phrase is visible: quote the complete phrase, logo, headline, caption, or metric (for example "most valuable", "company in the world", "website on the Internet", "The Rise", "however", or "$1 million"). A numeric counter must be called a view count, counter, or metric and include visible values. A discrete replacement of counter values may use mechanism cut even when the surrounding setup persists. Use type_on only for progressive character appearance; use scale or translate only when the layer visibly changes size or position.

Build unitEvidence as a static overview inventory. Record the underlying footage even when overlays dominate, and distinguish logo, map, interface, shape, texture, text, live_action_video, and archival_video. Quote readable text exactly. When the overview visibly shows a logo shrinking across samples, report a logo scale_down track with direction out; when a map grows across samples, report a map scale_up track with direction in. Those tracks must use the actual observed sample indices, span at least two intervals, and stop before any definite boundary.
